#include <stdio.h>

int main() {
    int n;

    // 사용자로부터 리스트의 크기를 입력받음
    printf("Enter the size of the list: ");
    scanf("%d", &n);

    // 배열을 이용하여 리스트 생성
    int list[n];

    // 사용자로부터 리스트의 각 원소를 입력받음
    printf("Enter %d integers:\n", n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &list[i]);
    }

    // 리스트의 합 계산
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += list[i];
    }

    // 결과 출력
    printf("Sum of the elements: %d\n", sum);

    return 0;
}

배열을 사용하여 리스트를 표현하고 리스트의 크기와 각 원소를 입력받아 합을 계산한다.
